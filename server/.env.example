# WebSocket 服务端口
WS_PORT=8080

# HTTP 健康检查端口（与 WS_PORT 相同）
HTTP_PORT=8080

# 最大房间数
MAX_ROOMS=200

# 房间空闲超时（秒），5 分钟 = 300
ROOM_TIMEOUT=300

# 日志级别：debug, info, warn, error
LOG_LEVEL=info

# 心跳间隔（秒）
HEARTBEAT_INTERVAL=30000

# 心跳超时（秒），超过此时间无响应视为断线
HEARTBEAT_TIMEOUT=60000

# 每个连接每秒最大消息数
RATE_LIMIT=10
